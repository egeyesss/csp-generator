## What changed

One or two sentences on what this PR does.

## Why

The motivation — what gap does this close, what regression does it prevent, what was the tradeoff.

## How it was tested

- [ ] `pytest` passes locally
- [ ] `ruff check` + `ruff format --check` clean
- [ ] `mypy src` clean
- [ ] If this affects clue distributions or difficulty, before/after numbers are in the description

## Notes for reviewers

Anything non-obvious worth flagging. Optional.
